__pycache__/
*.pyc
venv/
.venv/
*.egg-info/

# Keep the folder structure but don't force-track large local text data
# by default. Comment these out (or use `git add -f`) if you WANT to
# commit your trained checkpoint + data for deployment.
# data/*.txt
# checkpoints/*.pt
# checkpoints/*.json

.DS_Store
.streamlit/secrets.toml
